#ifndef PLAYER_H
#define PLAYER_H

#include "oslib/osspriteop.h"

typedef struct Player
{
	osspriteop_header *stillFrame;
	osspriteop_header *run0Frame;
	osspriteop_header *accFrame;
	osspriteop_header *run1Frame;
	osspriteop_header *jumpFrame;
	osspriteop_header *curFrame;
	int frame;
	osspriteop_trans_tab *mainTransTab, *jumpTransTab, *curTransTab;
	float x;
	float y;
	float xSpeed;
	float xAcc;
	float xMaxSpeed;
	float ySpeed;
	char facing;
	int isJumping;
}
Player;

typedef struct KeyCodes
{
	const int left;
	const int right;
	const int x;
}
KeyCodes;

typedef struct GameInfo
{
	const float gravity;
}
GameInfo;

void updatePlayer(Player *mario, osspriteop_area *area);

#endif